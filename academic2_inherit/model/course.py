#!/usr/bin/python
#-*- coding: utf-8 -*-

from odoo import models, fields, api, _
from odoo.exceptions import UserError

class course(models.Model):
    _name = "vit.course"
    _inherit = "vit.course"
